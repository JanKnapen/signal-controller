# SignalController Environment Configuration
# Copy this file to /etc/signal-controller/.env and edit with your values

# Your Signal phone number (with country code)
SIGNAL_PHONE_NUMBER=+1234567890

# API key for authentication (generate with: openssl rand -hex 32)
SIGNAL_API_KEY=CHANGE_ME_INSECURE_DEFAULT_KEY

# IP whitelist for private API (comma-separated, no spaces)
# Add IPs of VMs that should have access to send messages
# Example: 192.168.1.100,192.168.1.101,127.0.0.1
PRIVATE_API_WHITELIST=127.0.0.1

# Webhook URL for new message notifications (optional)
# SignalController will POST to this URL when a new message is received
# Example: http://192.168.1.100:8000/api/signal/new-message/
WEBHOOK_URL=

# Optional secret for webhook authentication
# Will be sent as X-Webhook-Secret header
WEBHOOK_SECRET=

# signal-cli REST API URL
SIGNAL_CLI_URL=http://localhost:8080

# Database path
DATABASE_PATH=/var/lib/signal-controller/messages.db
